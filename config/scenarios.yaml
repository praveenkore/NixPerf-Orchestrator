scenarios:
  - name: login_test
    jmx_path: scenarios/login.jmx
    load_steps: [500, 1000, 2000, 5000, 10000, 15000]
    rampup: 60              # seconds — passed as -Jrampup to JMeter
    sla:
      p95: 2000
      error_threshold: 50
